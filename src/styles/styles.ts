import { COLOR } from '../constants/colors';

export const boxShadow = {
  shadowColor: COLOR.BLACK,
  shadowOffset: {
    width: 0,
    height: 4,
  },
  shadowRadius: 2,
  shadowOpacity: 0.6,
  elevation: 2,
};
